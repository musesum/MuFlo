body {
    left {
        shoulder {
            elbow {
                wrist {
                    thumb {
                        meta.joint (x, y, z)
                        prox.joint (x, y, z)
                        dist.joint (x, y, z)
                        joint (x, y, z)
                    }
                    index {
                        meta.joint (x, y, z)
                        prox.joint (x, y, z)
                        dist.joint (x, y, z)
                        joint (x, y, z)
                    }
                    middle {
                        meta.joint (x, y, z)
                        prox.joint (x, y, z)
                        dist.joint (x, y, z)
                        joint (x, y, z)
                    }
                    ring {
                        meta.joint (x, y, z)
                        prox.joint (x, y, z)
                        dist.joint (x, y, z)
                        joint (x, y, z)
                    }
                    pinky {
                        meta.joint (x, y, z)
                        prox.joint (x, y, z)
                        dist.joint (x, y, z)
                        joint (x, y, z)
                    }
                    joint (x, y, z)
                }
                joint (x, y, z)
            }
            joint (x, y, z)
        }
        hip {
            knee {
                ankle {
                    toes.joint (x, y, z)
                    joint (x, y, z)
                }
                joint (x, y, z)
            }
            joint (x, y, z)
        }
        joint (x, y, z)
    }
    right {
        shoulder {
            elbow {
                wrist {
                    thumb {
                        meta.joint (x, y, z)
                        prox.joint (x, y, z)
                        dist.joint (x, y, z)
                        joint (x, y, z)
                    }
                    index {
                        meta.joint (x, y, z)
                        prox.joint (x, y, z)
                        dist.joint (x, y, z)
                        joint (x, y, z)
                    }
                    middle {
                        meta.joint (x, y, z)
                        prox.joint (x, y, z)
                        dist.joint (x, y, z)
                        joint (x, y, z)
                    }
                    ring {
                        meta.joint (x, y, z)
                        prox.joint (x, y, z)
                        dist.joint (x, y, z)
                        joint (x, y, z)
                    }
                    pinky {
                        meta.joint (x, y, z)
                        prox.joint (x, y, z)
                        dist.joint (x, y, z)
                        joint (x, y, z)
                    }
                    joint (x, y, z)
                }
                joint (x, y, z)
            }
            joint (x, y, z)
        }
        hip {
            knee {
                ankle {
                    toes.joint (x, y, z)
                    joint (x, y, z)
                }
                joint (x, y, z)
            }
            joint (x, y, z)
        }
        joint (x, y, z)
    }
    joint (x, y, z)
}
